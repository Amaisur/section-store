<style>
.star-sn {
  width: 100px; /* Set your desired width */
  height: 100px; /* Set your desired height */
  background: red;
  background-size: cover; /* Ensures the background image covers the star */
  background-position: center; /* Center the image inside the star */
  display: inline-block;
  clip-path: url(#star-shape-s); /* Use the clip-path to apply the star shape */

}
</style>
<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <mask id="half-star-mask">
      <!-- Full star with a half rectangle cut out -->
      <polygon points="50,10 61,35 98,35 68,57 79,91 50,70 21,91 32,57 2,35 39,35" fill="white" />
      <rect x="50" y="0" width="50" height="100" fill="black" />
    </mask>
  </defs>
  <polygon points="50,10 61,35 98,35 68,57 79,91 50,70 21,91 32,57 2,35 39,35" fill="yellow" mask="url(#half-star-mask)" />
  
  <!-- Background image applied to the star -->
  <rect width="100" height="100" fill="url(#half-star-mask)" mask="url(#half-star-mask)" />
</svg>

<div class="text-container">
{% for i in (1..5) %}
  {% render 'ss-stars-v1' %}
{% endfor %}
</div>
{% schema %}
  {
    "name": "Star component",
    "settings": [],
    "presets": [
      {
        "name": "Star component"
      }
    ]
  }
{% endschema %}