{% for block in section.blocks %}
{% assign prop = block.settings %}
{% endfor %}
{% render 'announcement-bar', text: prop.text  %}

{% schema %}
  {
    "name": "Announcement 2",
    "settings": [],
    "blocks": [
      {
        "type": "text",
        "name": "Announcement",
        "settings": [
          {
            "type": "text",
            "id": "text",
            "label": "Text",
            "default": "WORLD WIDE SHIPPING"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Announcement 2"
      }
    ]
  }
{% endschema %}